---
default: -p chrome
html: --format html --out report/others/features.html
dot: --format progress
bdd: --format pretty
json: --format json --out report/others/cucumber.json

# Browsers suportados
chrome: -p html -p bdd -p json BROWSER=chrome
firefox: -p html -p bdd -p json BROWSER=firefox